apply plugin: 'maven'
uploadArchives {
    repositories {
        mavenDeployer {
            pom.groupId = 'top.superbig'
            pom.artifactId = "httplibrary"
            pom.version = '1.0.8'
            repository(url:uri("C:\\Users\\Administrator\\.m2\\repository"))
        }
    }
}